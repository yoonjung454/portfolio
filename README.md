# Portfolio — 개인 기록용 포트폴리오 사이트 기획서

전자공학과 3학년 학생이 그동안 진행한 프로젝트와 학습 과정을 정리하기 위해 만드는 개인 포트폴리오 사이트 기획서다.

회로부터 코드까지 직접 만들어보면서 남긴 기록들을 한곳에 모아두는 **개인 아카이브 + 포트폴리오** 이다. 전자공학, 하드웨어, 임베디드, 프로그래밍, AI 등 프로젝트를 하면서 자연스럽게 넓어진 관심 분야를 보여주고, 결과물뿐 아니라 그 과정에서 부딪혔던 문제와 해결 방법도 함께 남기려고 한다.

---

## 목차

1. [전체 사이트 구조](#1-전체-사이트-구조)
2. [Home — 3D Interactive Hero](#2-home--3d-interactive-hero)
3. [About](#3-about)
4. [Current Project — 진행중인 프로젝트](#4-current-project--진행중인-프로젝트)
5. [Projects Archive](#5-projects-archive)
6. [Project Hover / Click Interaction](#6-project-hover--click-interaction)
7. [Skills](#7-skills)
8. [Experience](#8-experience)
9. [Contact](#9-contact)
10. [스크롤 애니메이션](#10-스크롤-애니메이션)
11. [기술 스택](#11-기술-스택)
12. [파일 구조](#12-파일-구조)
13. [파일별 역할](#13-파일별-역할)
14. [구현 가능 여부](#14-구현-가능-여부)
15. [구현 범위 및 제한사항](#15-구현-범위-및-제한사항)
16. [향후 확장 기능](#16-향후-확장-기능)

---

## 1. 전체 사이트 구조

사이트는 페이지 이동 없이 아래로 스크롤하면서 각 섹션을 순서대로 보여주는 **싱글 페이지(Single Page) 구조**로 만든다.

```
Navigation Bar (상단 고정)
│
├── 1. Home             — 3D 인터랙티브 히어로
├── 2. About            — 자기소개
├── 3. Current Project  — 진행중인 프로젝트 (피지컬 AI 일경험 프로젝트)
├── 4. Projects         — 프로젝트 아카이브 (9개)
├── 5. Skills           — 기술 스택
├── 6. Experience       — 교육 / 활동 / 수상
└── 7. Contact          — 연락처
```

메뉴를 클릭하면 페이지 이동 없이 해당 섹션으로 부드럽게 스크롤 이동한다. 지금 보고 있는 섹션이 어디인지에 따라 메뉴 항목이 자동으로 강조 표시되도록 만들 계획이다.

Current Project는 Projects Archive 바로 위에 배치해서, "지금 진행 중인 것"과 "이미 마무리한 것"이 시각적으로 자연스럽게 이어지도록 구성한다.

전체적으로는 "Home은 화려하고 인터랙티브하게, 나머지 섹션은 깔끔하고 읽기 편하게"라는 방향을 기본으로 잡았다. 인터랙션이 사이트의 개성이 되긴 해야 하지만, 그것 때문에 정작 프로젝트 내용을 읽기 어려워지면 안 된다고 생각한다.

---

## 2. Home — 3D Interactive Hero



### 2-1. 콘셉트


중앙 오브젝트의 구체적인 형태와 디테일은 아래 2-2에서 설명할 Spline에서 직접 만들어보면서 정할 예정이라, 지금은 전체적인 콘셉트만 잡아둔 상태다.

### 2-2. 3D 오브젝트 구현 방식 — Spline

중앙의 3D 오브젝트는 **Spline**(브라우저에서 3D 씬을 제작할 수 있는 디자인 툴)으로 만들어서 가져올 예정이다. Three.js 코드를 직접 짜는 대신, Spline에서 마우스로 오브젝트를 배치하고 애니메이션/인터랙션을 설정한 뒤, 완성된 씬을 웹페이지에 넣을 수 있는 임베드 코드(`<spline-viewer>` 웹 컴포넌트 + `<script>` 태그, 또는 iframe 형태)로 내보내는 방식이다.

- Spline 자체 기능으로 "마우스를 따라 오브젝트가 회전/기울어지는" 인터랙션을 씬 안에서 설정할 수 있어서, 중앙 오브젝트의 3D 회전은 별도의 JavaScript 계산 없이 Spline 쪽에서 처리될 가능성이 높다.
- 임베드 코드를 붙여넣는 것은 영상이나 지도를 삽입하는 것과 비슷한 수준이라, React/Vue 같은 프레임워크나 복잡한 빌드 도구 없이도 순수 HTML/CSS/JS 사이트에 넣을 수 있다.
- 다만 어떤 오브젝트를 어떻게 구성하고, 어떤 인터랙션을 넣을지 등 **구체적인 내용은 Spline에서 직접 만들어보면서 추후 결정**한다. 이 문서에는 방향성만 먼저 정리해둔다.
- 주변의 작은 요소들(Sensor, MCU, Code, AI 등 라벨)은 Spline 씬과 별개로, 기존에 계획했던 CSS `perspective` / `transform` / `mousemove` 기반의 Parallax 효과로 얹어서 서로 다른 레이어처럼 움직이게 할 수 있다.
- 만약 Spline 임베드가 성능이나 디자인 면에서 잘 맞지 않으면, 예전에 정리했던 **CSS 3D Transform 기반의 Tilt 효과**(`perspective`, `rotateX()`, `rotateY()`, `translateZ()`, `mousemove` 이벤트만으로 구현하는 방식)로 대체할 수 있도록 백업 플랜으로 남겨둔다.

### 2-3. Home에 들어갈 텍스트 요소

- 이름
- "전자공학과 학생" 소개 문구
- 한 줄 소개 — **추후 작성** (방향성만 참고: `From circuits to code.` / `I build ideas into systems.` 같은 톤. 아직 확정 문구는 아님)
- Projects(또는 Portfolio)로 이동하는 버튼/링크
- 아래로 스크롤하라는 표시 (예: 아래 화살표 아이콘, 살짝 위아래로 움직이는 애니메이션)

---

## 3. About

About은 Home의 화려한 3D 연출 다음에 나오는 만큼, 차분하게 나를 소개하는 섹션으로 구성한다. 담을 내용은 다음과 같다.

- 전자공학과 3학년이라는 것
- 전자공학과 소프트웨어를 함께 활용하는 프로젝트에 관심이 많다는 것
- 아이디어를 직접 기획하고 결과물로 만들어보는 걸 좋아한다는 것
- 기획력과 실행력이 강점이라는 것
- 프로젝트 도중 문제가 생겨도 끝까지 해결해서 마무리 짓는 성향이라는 것

Home에서 About으로 넘어올 때는 앞서 설명한 스크롤 전환 효과(오브젝트가 흩어지고 Fade Out)가 자연스럽게 이어지도록 하고, About 섹션 자체는 제목과 본문 텍스트가 아래에서 위로, 또는 Fade In으로 등장하는 정도의 차분한 애니메이션만 적용한다.

---

## 4. Current Project — 진행중인 프로젝트

Projects Archive는 기본적으로 **이미 마무리된 프로젝트**들을 서류철에 정리해두는 공간이다. 그런데 지금 한창 진행 중인 프로젝트(피지컬 AI 일경험 프로젝트)를 그 안에 그냥 같이 넣어두면, 끝난 기록들 사이에 미완성 프로젝트가 섞여서 오히려 어색해 보일 것 같았다. 그래서 Archive 바로 위에 **지금 진행 중인 프로젝트만 별도로 보여주는 섹션**을 하나 추가하기로 했다.

### 4-1. 이 섹션을 따로 두는 이유

- 방문자 입장에서 "지금 무엇을 하고 있는 사람인지"를 가장 먼저 보여줄 수 있다.
- 아직 완료되지 않은 프로젝트이기 때문에, 완결된 기록인 Archive와 디자인 톤을 구분하고 싶다.
- 프로젝트가 끝나면 이 섹션의 내용을 정리해서 Projects Archive 쪽으로 옮기고, Current Project 자리는 그다음 진행 중인 프로젝트로 교체하는 식으로 계속 갱신할 계획이다.

### 4-2. 디자인 방향

Projects Archive가 "닫혀 있는 서류철 파일"이라면, Current Project는 **아직 닫히지 않고 책상 위에 펼쳐져 있는 파일 / 진행 중인 보드** 같은 느낌으로 구분한다.

- `IN PROGRESS` 같은 배지(Badge)를 눈에 띄게 표시
- 진행 상태를 보여주는 요소 (예: 현재 단계 텍스트, 또는 간단한 진행률 바)
- Archive의 정적인 탭 UI와 달리, 살짝 강조되는 느낌의 은은한 애니메이션(예: 배지가 천천히 깜빡이거나 테두리가 은은하게 빛나는 정도)을 적용해서 "지금도 진행 중"이라는 인상을 준다
- 다만 과하게 화려한 효과보다는 Home의 3D 인터랙션과 겹치지 않는 선에서, 차분하지만 눈에 띄는 정도로만 구성

### 4-3. 표시할 내용

- 프로젝트명: 피지컬 AI 일경험 프로젝트
- 참여 형태 / 소속 기관·프로그램명: **작성 예정**
- 진행 기간: 시작일 ~ 진행중 (종료일 없이 "진행중" 표시)
- 프로젝트 개요 (한 줄 소개): **작성 예정**
- 담당 역할: **작성 예정**
- 사용 중인 / 사용 예정 기술: **작성 예정**
- 현재까지 진행 상황: 짧은 로그(day/week 단위 기록) 형태로 정리 예정
- 앞으로의 계획: **작성 예정**
- GitHub 링크: 저장소가 준비되면 연결 (현재는 placeholder)

### 4-4. 구현 방식

새로운 기술이 필요한 영역은 아니고, 앞서 정리한 스크롤 애니메이션(등장 효과)과 File Folder UI 스타일을 그대로 재활용해서 구현할 수 있다. 차이가 있다면 `is-in-progress` 같은 class를 하나 추가해서, 배지 표시나 은은한 강조 애니메이션 정도만 CSS로 다르게 적용해주는 수준이다. 즉 별도의 새 기술 없이 **기존 CSS/JS 구조 위에 상태 표시만 얹는 방식**으로 충분히 구현 가능하다.

---

## 5. Projects Archive

총 **9개의 프로젝트**를 보여줄 예정이다. 이 부분은 이번 사이트에서 Home 다음으로 신경 쓰고 싶은 영역인데, 일반적인 카드 Grid 대신 **서류철(파일 캐비닛)에서 프로젝트 파일을 한 장씩 꺼내보는 느낌**으로 만들고 싶다.

참고하는 이미지 키워드는 File Folder Tab UI, File Cabinet UI, Stacked Folder Tabs, Index Card Interface, Folder Tab Hover Animation 등이다.

Home이 "전자공학 / 기술 / 인터랙티브"한 느낌이라면, Projects는 "내가 진행한 프로젝트들을 모아둔 기록 보관함" 느낌으로 톤을 다르게 가져갈 생각이다. 같은 사이트 안에서 섹션마다 분위기가 조금 달라도, 오히려 Home = 미래적인 도입부 / Projects = 실제 기록 보관소 라는 구분이 명확해져서 괜찮다고 판단했다.

### 5-1. 기본 화면 — 겹쳐진 파일 탭

9개의 프로젝트가 파일 탭처럼 위아래로 살짝 겹쳐진 형태로 쌓여있다.

```
PROJECT ARCHIVE

┌──── PROJECT 01 ──────────────┐
├──── PROJECT 02 ──────────────┐
├──── PROJECT 03 ──────────────┐
├──── PROJECT 04 ──────────────┐
├──── PROJECT 05 ──────────────┐
├──── PROJECT 06 ──────────────┐
├──── PROJECT 07 ──────────────┐
├──── PROJECT 08 ──────────────┐
└──── PROJECT 09 ──────────────┘
```

각 탭에는 다음 정보를 짧게 표시한다.

- 프로젝트 번호 (01 ~ 09)
- 프로젝트 이름
- 프로젝트 분야 (예: 임베디드, AI, 웹 등)
- 짧은 태그 (예: `#Arduino` `#센서` 형태)

---

## 6. Project Hover / Click Interaction

### 6-1. Hover — 파일이 살짝 빠져나오는 효과

마우스를 특정 프로젝트 탭 위에 올리면, 그 파일만 서류철에서 살짝 앞으로 빠져나온 것처럼 보이게 한다.

- 탭이 위쪽 또는 옆쪽으로 살짝 이동
- `z-index`를 높여서 다른 탭 위로 겹쳐 보이게 함
- 프로젝트 제목 강조 (색상/굵기 변화)
- 탭이 조금 더 펼쳐지는 느낌의 크기 변화
- 자연스러운 `transition`으로 위 변화들이 부드럽게 일어나도록 함

사용하는 CSS는 `:hover`, `transform`, `translate`, `transition`, `z-index` 정도로, 모두 지금까지 배운 범위 안에서 구현 가능하다.

### 6-2. Click — 파일을 꺼내서 펼쳐보는 상세 화면

탭을 클릭하면 그 프로젝트를 실제로 꺼내서 펼쳐보는 것 같은 느낌으로 상세 내용이 나타난다. 새 페이지로 이동하는 대신, **같은 페이지 안에서 JavaScript로 상세 영역을 열고 닫는 방식**을 기본으로 한다.

구현 방식 후보:

- 클릭한 탭 아래쪽에 상세 영역이 펼쳐지는 방식 (아코디언과 비슷한 구조)
- 또는 화면에 별도의 Project Detail 영역이 나타나는 방식 (오버레이/모달과 비슷한 구조)

두 방식 모두 JavaScript `click` 이벤트로 대상 프로젝트를 확인하고, 해당 요소에 `is-open` 같은 class를 추가/제거하면서 CSS `transition`으로 펼쳐지고 닫히는 애니메이션을 주는 식으로 구현한다. 이미 배운 이벤트 처리 + class 조작 방식으로 충분히 만들 수 있는 수준이라고 판단했다.

### 6-3. 프로젝트 상세 내용 항목

각 프로젝트를 클릭했을 때 보여줄 항목은 다음과 같다.

- 프로젝트 이름
- 대표 이미지
- 프로젝트 기간
- 한 줄 소개
- 프로젝트를 시작한 이유
- 담당 역할
- 사용 기술
- 구현 내용
- 문제점 및 해결 과정
- 결과
- 느낀 점
- GitHub 링크
- 사진 또는 영상

### 5-2 / 6-4. 프로젝트 템플릿 (Project 01 ~ 09)

아직 각 프로젝트의 세부 내용은 정리 전이라, 아래 템플릿 형태로만 자리를 잡아둔다. 프로젝트가 정리되는 대로 하나씩 채워 넣을 예정이다.

```
Project 0X — (프로젝트 이름 예정)
분야        : (예: 임베디드 / AI / 웹 등)
기간        : (예: 2025.0X ~ 2025.0X)
한 줄 소개   : 작성 예정
시작한 이유  : 작성 예정
담당 역할    : 작성 예정
사용 기술    : 작성 예정
구현 내용    : 작성 예정
문제 & 해결  : 작성 예정
결과        : 작성 예정
느낀 점      : 작성 예정
GitHub      : 링크 예정
이미지/영상   : 추가 예정
```

이 템플릿을 Project 01부터 Project 09까지 동일하게 적용한다. 참고로 Current Project(피지컬 AI 일경험 프로젝트)가 마무리되면, 이 템플릿 형식 그대로 정리해서 Archive의 다음 번호로 옮겨올 계획이다.

---

## 7. Skills

기술 스택은 카테고리별로 정리한다. 각 항목은 "전문가 수준"이 아니라 **프로젝트나 실습을 하면서 실제로 사용해본 기술**이라는 뉘앙스로 표현할 예정이다.

### Programming

- Python
- C
- JavaScript

### Web

- HTML
- CSS
- JavaScript

### Hardware / Embedded

- Arduino
- ESP32
- 센서 활용
- 회로 구성

### AI / Computer Vision

- YOLO
- TensorFlow.js
- Teachable Machine

### Tools

- Git
- GitHub
- VS Code
- Jupyter Notebook

---

## 8. Experience

Experience는 아직 상세 내용을 정하지 않아서, 나중에 채워 넣을 수 있도록 구조만 먼저 잡아둔다.

- 교육 — *추후 추가 예정*
- 대외활동 — *추후 추가 예정*
- 공모전 — *추후 추가 예정*
- 수상 — *추후 추가 예정*
- 기타 활동 — *추후 추가 예정*

---

## 9. Contact

연락처 정보는 아래 항목을 실제 주소가 정해지면 연결한다. 지금은 placeholder로만 표시한다.

- GitHub: `https://github.com/username` (placeholder)
- Email: `example@email.com` (placeholder)
- Blog: `https://blog-address` (placeholder)
- 기타 링크: 추후 추가 예정

이 사이트는 서버와 데이터베이스를 사용하지 않기 때문에, 문의 내용을 입력받아 저장하는 Contact Form 기능은 이번 범위에서는 제외한다. Contact 섹션은 각 링크로 바로 이동하는 형태로만 구성한다.

---

## 10. 스크롤 애니메이션

스크롤 애니메이션은 이 사이트에서 인터랙션 다음으로 중요한 요소다. 다음 기능들을 포함할 계획이다.

- 각 섹션 진입 시 자연스럽게 등장하는 효과
- 제목이 아래에서 위로 올라오며 등장
- 설명 텍스트 Fade In
- Skills 항목들이 순차적으로 하나씩 등장
- Current Project 진입 시 배지 및 상태 요소가 은은하게 강조되며 등장
- Projects 진입 시 파일 탭들이 순서대로 등장
- 현재 스크롤 위치에 따라 Navigation 메뉴가 자동으로 활성화
- Navigation 클릭 시 Smooth Scroll로 이동
- Home의 3D 오브젝트에서 About으로 넘어가는 전환 효과

이 기능들은 대부분 **Vanilla JavaScript의 `IntersectionObserver`**를 기반으로 구현한다. 각 섹션(또는 섹션 안의 요소)이 화면에 들어오는 시점을 감지해서 `visible` 같은 class를 추가하고, 실제 등장 애니메이션은 CSS `transition` / `animation`으로 처리하는 방식이다. Navigation 활성화도 같은 방식으로, 현재 화면에 보이는 섹션이 무엇인지 감지해서 해당 메뉴에 `active` class를 붙이는 식으로 구현할 수 있다.

---

## 11. 기술 스택

### 사용하는 기술

- HTML
- CSS
- JavaScript (Vanilla JS)
- Spline (Home 3D 오브젝트 제작 및 임베드 전용 — 코드를 직접 작성하는 프레임워크가 아니라, 완성된 씬을 가져오는 외부 툴)

### 사용하지 않는 기술

- 서버 / 백엔드
- 데이터베이스
- React, Vue 등 프론트엔드 프레임워크
- Node.js 기반 백엔드
- 로그인 / 회원가입 기능
- 복잡한 빌드 도구나 프레임워크
- Three.js를 직접 코드로 작성하는 방식 (필수 기술 아님 — 향후 확장 기능으로만 고려)

Home의 3D 화면은 Spline에서 만든 씬을 임베드 코드로 가져오는 방식을 기본으로 하고, 주변 요소들은 **CSS 3D Transform + JavaScript 마우스 인터랙션**으로 보완한다. Spline 임베드 자체는 영상/지도를 삽입하는 정도의 코드 추가이기 때문에, React나 Three.js를 직접 배우지 않아도 지금까지 배운 HTML/CSS/JS 범위 안에서 사이트를 완성할 수 있다는 원칙은 그대로 유지된다.

---

## 12. 파일 구조

```
portfolio/
├── index.html
├── css/
│   └── style.css
├── js/
│   └── main.js
├── images/
│   ├── profile/
│   ├── home/
│   ├── current-project/
│   └── projects/
│       ├── project01/
│       ├── project02/
│       ├── project03/
│       └── ...
└── README.md
```

`images/current-project/` 폴더는 지금 진행 중인 피지컬 AI 일경험 프로젝트의 사진/자료를 모아두는 용도로 추가했다. 나중에 이 프로젝트가 끝나면 이 폴더 내용을 정리해서 `images/projects/project10/` 같은 형태로 옮길 예정이다.

현재 규모(싱글 페이지 + 정적 리소스)에서는 이 정도 구조면 충분하다고 판단했다. 파일을 더 잘게 쪼개는 것도 고려했지만, 혼자 관리하는 개인 프로젝트인 만큼 지금은 `css`, `js`, `images` 정도로만 나누고 관리 편의성을 우선하기로 했다.

---

## 13. 파일별 역할

### `index.html`

- Navigation 구조
- Home / About / Current Project / Projects / Skills / Experience / Contact 각 섹션의 HTML 마크업
- Home 3D 오브젝트의 HTML 구조 (Spline 임베드 요소 + 주변 요소)
- Current Project 섹션의 상태 배지 및 진행 정보 구조
- Project Archive의 탭 구조 (9개 프로젝트 + 각 상세 영역)

### `css/style.css`

- 사이트 전체 레이아웃과 디자인
- 반응형 레이아웃 (미디어 쿼리)
- Home의 Spline 임베드 영역 레이아웃 및 주변 요소의 `perspective` / `transform` 관련 스타일
- Hover 효과 스타일
- 스크롤 애니메이션에 사용되는 class 스타일 (예: `.visible`, `.active`)
- Current Project의 `IN PROGRESS` 배지 및 은은한 강조 애니메이션 스타일
- File Folder(파일 탭) UI 스타일
- Project Archive 전체 디자인

### `js/main.js`

- `mousemove` 이벤트 감지 및 Home 주변 요소 Parallax 효과 처리 (중앙 3D 오브젝트 자체의 회전은 Spline 씬 내부 설정으로 처리하고, 별도 JS 보정이 필요하면 여기서 추가)
- 스크롤 위치 계산 및 Home → About 전환 효과 처리
- `IntersectionObserver`를 이용한 섹션 등장 애니메이션 처리
- 스크롤 위치 기반 Navigation 활성화
- 프로젝트 탭 클릭 이벤트 처리
- 프로젝트 상세 영역 열기/닫기 (class 추가·제거)

### `images/home/`

- Home에 사용할 PCB, 회로, 전자 부품 관련 이미지 또는 그래픽 요소 (중앙 3D 오브젝트는 Spline 임베드로 대체되므로, 여기에는 주변 요소용 이미지나 CSS 3D Tilt로 대체할 경우를 대비한 백업용 이미지를 넣어둘 예정)

### `images/current-project/`

- 진행 중인 피지컬 AI 일경험 프로젝트 관련 사진/자료

### `images/projects/`

- 프로젝트별 대표 이미지
- 진행 과정 사진
- 결과물 이미지

각 프로젝트 폴더(`project01` ~ `project09`)에 해당 프로젝트 이미지를 모아둔다.

### `README.md`

- 이 문서. 사이트 기획, 구조, 구현 기능, 기술 범위, 구현 가능 여부를 정리해둔 설계 문서.

---

## 14. 구현 가능 여부

기획한 기능들이 지금까지 배운 HTML / CSS / JavaScript 범위 안에서 실제로 구현 가능한지 기능별로 정리했다.

| 기능 | HTML | CSS | JS | 구현 가능 여부 |
|---|---|---|---|---|
| Home 중앙 3D 오브젝트 | 필요 (임베드 태그) | 필요 (레이아웃/사이즈) | 불필요~선택 (회전은 Spline 씬 내부 설정, 보정 시에만 JS 추가) | 가능 — Spline 임베드 기반 (구체적인 씬은 추후 결정, 안 되면 CSS Tilt로 대체) |
| Mouse Interaction (주변 요소) | - | 필요 (`transform`, `transition`) | 필요 (`mousemove`, 좌표 계산) | 가능 |
| Parallax (주변 요소) | 필요 | 필요 (`transform`) | 필요 (요소별 이동 비율 계산) | 가능 |
| Scroll Animation | 필요 | 필요 (`transition`, `animation`) | 필요 (`IntersectionObserver`) | 가능 |
| Smooth Scroll | 필요 (앵커 링크) | 필요 (`scroll-behavior: smooth` 또는 JS 처리) | 필요 시 (`scrollIntoView`) | 가능 |
| Navigation 활성화 | 필요 | 필요 (`.active` 스타일) | 필요 (`IntersectionObserver`) | 가능 |
| Current Project 상태 배지 | 필요 | 필요 (배지 스타일, 은은한 강조 애니메이션) | 선택 (없어도 동작, 있으면 강조 연출 추가) | 가능 |
| File Folder UI | 필요 | 필요 (레이아웃/겹침 구조) | - | 가능 |
| Folder Hover Animation | - | 필요 (`:hover`, `transform`, `z-index`) | - | 가능 |
| Project Click Interaction | 필요 | 필요 (`transition`) | 필요 (`click` 이벤트) | 가능 |
| Project Detail Open/Close | 필요 | 필요 (펼침 애니메이션) | 필요 (class 추가/제거) | 가능 |
| 프로젝트 이미지 표시 | 필요 (`img`) | 필요 (사이즈/레이아웃) | - | 가능 |
| Skills 섹션 | 필요 | 필요 (카테고리별 레이아웃, 순차 등장 스타일) | 필요 시 (`IntersectionObserver`로 순차 등장) | 가능 |
| Contact 링크 | 필요 (`a` 태그) | 필요 (스타일) | - | 가능 |
| 반응형 디자인 | 필요 | 필요 (미디어 쿼리) | - | 가능 |

특히 강조하고 싶은 부분은, **Three.js 코드를 직접 짜지 않아도 Spline 같은 툴에서 만든 3D 씬을 임베드 코드만으로 가져와 실제 3D 화면을 만들 수 있다는 점**이다. 임베드 코드를 붙여넣는 것 자체는 영상이나 지도를 삽입하는 정도의 작업이라 별도의 3D 프로그래밍 지식이 없어도 되고, 그 위에 얹는 주변 요소나 스크롤 연동 효과는 지금까지 배운 CSS `perspective` / `transform`과 JavaScript로 충분히 구현할 수 있다. 만약 Spline 임베드가 여의치 않은 상황이 오더라도, 평면 요소를 3D 공간처럼 배치하고 마우스 좌표에 따라 회전각만 계산하는 CSS 3D Transform 방식으로 대체할 수 있어 이중으로 안전장치를 마련해둔 셈이다.

---


## 15. 향후 확장 기능

아래 기능들은 이번 프로젝트와는 분리해서, 나중에 여유가 생기면 시도해보고 싶은 확장 아이디어로 정리해둔다.

- Spline 없이 Three.js 코드를 직접 작성해서 커스텀 3D Scene 만들어보기
- Spline 씬을 더 정교한 3D PCB 모델로 고도화
- WebGL 기반 그래픽 효과
- Backend 서버 구축
- Database 연동
- 관리자 페이지 (프로젝트 관리용)
- 프로젝트 자동 추가/관리 기능
- Contact Form 문의 내용 저장 기능
- Current Project 진행 상황을 별도 JSON/파일로 분리해서 관리하는 구조 (지금은 HTML에 직접 작성)

지금은 위 기능들 없이도 완성도 있는 포트폴리오 사이트를 만드는 것이 우선이고, 위 항목들은 사이트가 어느 정도 자리를 잡은 뒤 하나씩 시도해볼 예정이다.
