app.py와 손코딩으로 작성한 내용 비교분석

|app.py|손코딩|
|-|-|
|날씨와 대기질을 한 번에 조회하고 외출 가능 여부판단|날씨와 대기질 각각 따로 조회|
|명령어에 도시가 없으면 input()으로 다시 입력받음<br />여러 함수 사용<br />도시를 찾지 못하거나 프로그램이 종료되지 않도록 처리<br />if문으로 기온,강수확률,AQI를 판단|<br />도시가 없으면 기본값 Seoul 사용<br />find\_city()만 함수로 만들고 나머지는 순서대로 작성<br />날씨는 수치만 출력. 대기질은 등급으로 출력 |
|도시 입력(없으면 다시 입력 요청 → 위도·경도 검색 → 날씨·대기질 조회 → 두 결과를 judge()에 전달 → 외출 가능 여부 출력|(air.py): 도시 입력(없으면 서울) → 위도·경도 검색 → 대기질 조회 → PM2.5 기준 등급 판정 → 결과 출력|







&#x09;





















